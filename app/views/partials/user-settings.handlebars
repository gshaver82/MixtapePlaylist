<div class="modal fade" id="deleteWarning" data-playlist="" tabindex="-1" role="dialog"
    aria-labelledby="deleteWarningLabel" aria-hidden="true">
    <div class="modal-dialog" role="document">
        <div class="modal-content">
            <div class="modal-header">
                <h3 class="modal-title font-marker" id="deleteWarningLabel">Delete Playlist</h3>
                <button type="button" class="close text-white" data-dismiss="modal" aria-label="Close">
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                <p>Are you sure you want to delete this playlist? Once it is deleted it is gone forever.</p>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-dark font-block pt-2" data-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-danger font-block pt-2" id="confirm-delete">Confirm</button>
            </div>
        </div>
    </div>
</div>

<div class="container">
    <div class="card text-white bg-xtradark my-4">
        <div class="card-header">
            <div class="font-comment h2 row no-gutters align-items-end">
                <div class="col-md-fluid font-block display-1">Your Info</div>
                <div class="col-md-fluid mx-4 align-self-center"> for </div>
                <div class="col-md-fluid font-block display-3 pb-1 random-color">{{User.dataValues.username}}</div>
            </div>
            <hr class="mt-0">
            <div class="card-body py-0 font-marker row" id="info-panel">
                <div class="col">
                    <div class="row">
                        <div class="col-lg card-text">
                            <div class="text-muted mb-1">First Name :</div>
                            <div class="font-block h2 m-0">{{User.dataValues.first_name}} </div>
                            <div class="text-muted mb-1">Last Name :</div>
                            <div class="font-block h2 m-0">{{User.dataValues.last_name}} </div>
                        </div>
                        <div class="col-lg card-text">
                            <div class="text-muted mb-1">Username :</div>
                            <div class="font-block h2 m-0">{{User.dataValues.username}}</div>
                            <div class="text-muted mb-1">Email :</div>
                            <div class="font-block h2 m-0">{{User.dataValues.email}}</div>
                        </div>
                    </div>
                </div>
                <div class="col">
                    <div class="row">
                        <div class="col-lg card-text">
                            <div class="text-muted mb-1">Date Created :</div>
                            <div class="font-block h2 m-0" data-date="{{User.dataValues.createdAt}}" id="date-created">
                            </div>
                            <div class="text-muted mb-1">Last login :</div>
                            <div class="font-block h2 m-0" data-date="{{User.dataValues.last_login}}" id="last-login">
                            </div>
                        </div>
                        <div class="col-lg card-text">
                            <div class="text-muted mb-1">Total upvoted :</div>
                            <div class="font-block h2 m-0">{{dataValues.user_total_upvotes}}</div>
                            <div class="text-muted mb-1">Total downvoted :</div>
                            <div class="font-block h2 m-0">{{dataValues.user_total_downvotes}}</div>
                        </div>
                    </div>
                </div>
            </div>
            <hr class="mb-1">
            <div class="hidden toggle">
                <form class="card-body d-flex flex-wrap p-0" id="edit-panel">
                    <div class="card-text col-lg p-0 mx-1">
                        <label for="username" class="text-muted font-marker my-1">Username :</label>
                        <input type="text" class="form-control" id="username"
                            value="{{User.dataValues.username}}"></input>
                        <div class="invalid-feedback">Field cannot be left blank.</div>
                    </div>
                    <div class="card-text col-lg p-0 mx-1">
                        <label for="email" class="text-muted font-marker my-1">Email :</label>
                        <input type="email" class="form-control" id="email" value="{{User.dataValues.email}}"></input>
                        <div class="invalid-feedback">Field cannot be left blank.</div>
                    </div>
                    <div class="card-text col-lg p-0 mx-1">
                        <label for="firstName" class="text-muted font-marker my-1">First Name :</label>
                        <input type="text" class="form-control" id="firstName"
                            value="{{User.dataValues.first_name}}"></input>
                        <div class="invalid-feedback">Field cannot be left blank.</div>
                    </div>
                    <div class="card-text col-lg p-0 mx-1">
                        <label for="lastName" class="text-muted font-marker my-1">Last Name :</label>
                        <input type="text" class="form-control" id="lastName"
                            value="{{User.dataValues.last_name}}"></input>
                        <div class="invalid-feedback">Field cannot be left blank.</div>
                    </div>
                </form>
                <hr>
            </div>
            <div class="">
                <button type="button" class="btn btn-dark btn-xlg font-block pt-2 mt-2 toggle-fade" id="edit">Edit my
                    Information</button>
                <button type="button" class="btn btn-dark btn-xlg font-block pull-right pt-2 ml-2 toggle-fade hidden"
                    id="cancel">Cancel</button>
                <button type="button" class="btn btn-primary btn-xlg font-block pull-right pt-2 toggle-fade hidden"
                    id="submit">Submit Changes</button>
                <div class="pull-right mx-4">
                    <div class="alert alert-danger py-2 alert-dismissible" role="alert" id="db-alert">
                        <h3 class="font-block mb-0 mt-1" id="alert-text">Test</h3>
                        <button type="button" class="close anti-button" data-hide="alert" aria-label="Close">
                            <i class="fa fa-times text-danger"></i>
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>